PUBLIC_API_URL=http://localhost:3000

# Leave empty to disable Google sign-in. Must match a Web client ID
# whose authorized JavaScript origin is the frontend URL.
PUBLIC_GOOGLE_CLIENT_ID=

# Leave empty to disable Facebook sign-in. The Facebook app must list
# the frontend URL as an allowed domain.
PUBLIC_FACEBOOK_APP_ID=
PUBLIC_FACEBOOK_API_VERSION=v21.0

# Sign in with Apple — PUBLIC_APPLE_SERVICE_ID is the Services ID from
# the Apple Developer console. PUBLIC_APPLE_REDIRECT_URI must be one of
# the URLs registered for that Services ID; popup mode still needs this
# value to match exactly. Leave SERVICE_ID empty to disable.
PUBLIC_APPLE_SERVICE_ID=
PUBLIC_APPLE_REDIRECT_URI=
